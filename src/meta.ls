#------------------------------------------------------------------------------
# Meta data

export language = \livescript
export title = "#{language}-preview"
export uri = -> "#title:///#it LiveScript Preview"
